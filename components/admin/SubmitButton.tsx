import React from 'react'
import { Button } from '../ui/button'

type Props = {}

const SubmitButton = (props: Props) => {
	return <Button type='submit' className='w-full'>Submit</Button>
}

export default SubmitButton
